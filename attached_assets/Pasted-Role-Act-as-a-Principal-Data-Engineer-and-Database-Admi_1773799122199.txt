Role: Act as a Principal Data Engineer and Database Administrator.

Context: Our Sponsor Monitor job on checkbyai.net is hard-crashing. I have two problems to solve:

A hard database crash: column "granted_at" does not exist.
The data fetching architecture is weak. I want to implement the Scrapling library (https://github.com/D4Vinci/Scrapling) to dynamically find the CSV link, with a fallback to scrape the HTML online view if the CSV link is missing.
Task Step 1: Fix the Hard Blocker (DB Schema)
Scan the database schema/models for the Sponsor or Snapshot table.
Action: Write the exact SQL migration script or ORM command required to add the missing granted_at (Timestamp/DateTime) column. We cannot proceed until this is fixed.

Task Step 2: Implement the Scrapling Dynamic Fetcher
Scan the current fetch logic. Replace it with Scrapling.
Logic:

Use Scrapling to fetch the main Gov.uk Sponsor Register page.
Target the <a> tag that contains the text "Download CSV" or ends with .csv.
Extract that URL dynamically (since Gov.uk changes the URL daily).
Task Step 3: Stream the CSV (Memory Safe)
Once Scrapling gets the dynamic CSV URL, download it using a streamed request (stream=True). Do not load the 10.4MB file into memory all at once. Parse the CSV in chunks, mapping the columns to our database model (handling Windows-1252 encoding if utf-8 fails).

Task Step 4: The HTML Fallback Logic
If Scrapling fails to find the CSV link, implement a fallback mechanism.
Logic: Use Scrapling to parse the HTML <table> on the Gov.uk page. Note: Gov.uk only previews the first 1,000 rows in HTML. If the fallback triggers, log a critical warning: "WARNING: Falling back to HTML preview. Only 1,000 records will be synced." Parse the <tr> and <td> elements and pass them to the chunked database committer.

Execution Protocol:
Generate a structured plan for these 4 steps.
End your response with this question: "Plan ready. Shall I execute Step 1 to write the Database Migration for the granted_at column first?