Add the following UI/UX enhancements and access controls to the Sponsor Licence Monitor feature. Implement these changes step by step:

1. BACKEND: Enforce Paid-Only Watchlist Creation
   - Update the POST /api/watches endpoint.
   - Before creating a watch, check the user's plan/subscription tier.
   - If the user is on the 'free' plan (or has no active subscription), return a 403 status with the message: "Upgrade to Starter plan to add companies to your watchlist. Free users can view search results and history only."
   - Ensure the GET /api/sponsors/search endpoint remains accessible to all users (free and paid) for viewing/searching.

2. BACKEND: Company History Endpoint
   - Create GET /api/sponsors/:fingerprint/history endpoint (public or auth-required, but accessible to free users).
   - Query the sponsor_changes table for records matching the fingerprint.
   - Return the history sorted by detected_at DESC (newest first).
   - For each record, return: { id, date (detected_at), event (change_type), location (town_city), tier (type_rating), previousValue, newValue }.
   - Also include the current status from sponsor_canonical (ACTIVE or NOT_LISTED) in the response.

3. BACKEND: Ensure Search Returns Status
   - Update GET /api/sponsors/search to include the current status field (ACTIVE or NOT_LISTED) from sponsor_canonical for each result. This is needed for color coding on the frontend.

4. FRONTEND: Status Color Coding (Tailwind)
   - In the search results and watchlist cards, display a status badge:
     - If status === 'ACTIVE': Green badge using classes `bg-green-100 text-green-800 border-green-200`. Label: "Active".
     - If status === 'NOT_LISTED': Red badge using classes `bg-red-100 text-red-800 border-red-200`. Label: "Removed/Revoked".
     - If type_rating includes 'B-rated': Show an additional Amber badge `bg-amber-100 text-amber-800` with label "B-Rated (Compliance Issues)" next to the status.
   - Use the Badge component from shadcn/ui or standard Tailwind classes with rounded-full and px-2 py-1 styling.

5. FRONTEND: History Timeline Component
   - Create a new component CompanyHistory that displays the company's event history.
   - Display it as a vertical timeline or a clean table with columns: Date, Event, Location, Tier.
   - Map the events to human-readable labels:
     - 'ADDED' → "Licence Granted"
     - 'REMOVED' → "Licence Revoked / Removed from Register"
     - 'DOWNGRADED' → "Downgraded to B-Rating"
     - 'UPGRADED' → "Upgraded to A-Rating"
     - 'NAME_CHANGED' → "Company Name Updated"
   - Show this component on the company detail view or in a modal when a user clicks "View History" from the search results or watchlist.
   - For free users: Show the first 3 history events with a blur or overlay saying "Upgrade to view full history" (or just show all history but keep the "Add Watch" button disabled - your choice, but the prompt implies view is free, only watch is paid. So show full history to all).
   - Style the timeline with subtle borders (border-l-2 border-gray-200) and icons for each event type (e.g., CheckCircle for Added, AlertTriangle for Removed, ArrowDown for Downgraded).

6. FRONTEND: External Search Links
   - In the company detail/search result card, add two external link buttons styled as outline buttons or text links:
     - "View on Gov.uk": Link to `https://www.gov.uk/government/publications/register-of-licensed-sponsors-workers` (opens in new tab).
     - "Search on LinkedIn": Link to `https://www.linkedin.com/search/results/companies/?keywords=${encodeURIComponent(companyName)}` (opens in new tab).
   - Use lucide-react icons (ExternalLink, Linkedin) next to the text.

7. FRONTEND: Conditional Watchlist Button
   - In the search results, update the "Add to Watchlist" button logic:
     - If user has paid plan (Starter, Pro, Unlimited): Show the active "Add to Watchlist" button (shadcn Button variant default or outline).
     - If user is on Free plan: Show a disabled or secondary button saying "Upgrade to Monitor" or simply hide the add button and show a text link "Upgrade to add watches" that links to /pricing.
   - Ensure the UI clearly communicates that free users can view/search but cannot save watches.

8. DATA CHECK (Optional but recommended)
   - Verify that the sponsor_changes table is being populated correctly with the town_city and type_rating data during the daily cron job so that the History endpoint has complete data to display.

Use the existing React, Tailwind, shadcn/ui, and TanStack Query patterns already in the codebase. Ensure the color coding matches the "brutalist editorial" design system mentioned in the original tech spec (high contrast, clear semantic colors).