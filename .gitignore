node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.claude/
# ETL SQLite snapshots — environment-local, not for source control
backend/etl_sponsors.db
# Pipeline binaries (installed by scripts/setup-binaries.sh)
bin/
# Daily CSV archive files (large binary data, not for source control)
data/