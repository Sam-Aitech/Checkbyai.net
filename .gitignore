node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.claude/
# ETL SQLite snapshots — environment-local, not for source control
backend/etl_sponsors.db
# Platform-specific binaries — only unix variants are tracked
*.exe

# Test coverage output
coverage
